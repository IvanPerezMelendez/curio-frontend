services:

  frontend:
    build:
      context: .
      args:
        # URL pública del backend, ej: https://api.tudominio.com
        VITE_API_URL: ${VITE_API_URL}
    restart: unless-stopped
    expose:
      - "80"
